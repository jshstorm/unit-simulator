# Agentic vs Unit-Simulator: 빠른 비교

## 개요

이 문서는 agentic 파일럿 프로젝트와 unit-simulator 간의 빠른 참조 비교를 제공하며, 마이그레이션이 필요한 항목을 강조합니다.

---

## 1. 디렉토리 구조 비교

### Agentic (소스)
```
agentic/
├── .claude/
│   ├── agents/           ⭐ 5개 에이전트 정의
│   ├── commands/         ⭐ 4개 CLI 명령어
│   └── skills/           ⭐ 5개 스킬 (1122줄)
├── apps/backend/         📦 Express.js 앱
├── specs/                ⭐ 생성된 명세서
├── mcp.json             ⭐ MCP 설정
├── AGENTS.md            ⭐ 에이전트 운영 규칙
├── CLAUDE.md            ⭐ 행동 규칙
└── agentic_workflow.md  ⭐ 워크플로우 문서
```

### Unit-Simulator (타겟)
```
unit-simulator/
├── .claude/
│   └── settings.local.json  ✅ 권한만 설정됨
├── UnitSimulator.Core/      📦 C# 시뮬레이션 엔진
├── UnitSimulator.Server/    📦 WebSocket 서버
├── ReferenceModels/         📦 데이터 모델
├── sim-studio/              📦 React 대시보드
├── docs/                    ✅ 문서
└── (에이전트 인프라 없음)
```

**범례**:
- ⭐ = 마이그레이션 필요
- ✅ = 이미 존재 (agentic과 다름)
- 📦 = 애플리케이션 코드 (직접 마이그레이션 안 됨)

---

## 2. 생성해야 할 주요 파일

| 파일 | 목적 | 예상 줄 수 | 우선순위 |
|------|------|-----------|----------|
| `mcp.json` | MCP 서버 & 리소스 설정 | 50 | P0 |
| `AGENTS.md` | 에이전트 역할, 규칙, 핸드오프 프로토콜 | 400 | P0 |
| `CLAUDE.md` | 행동 규칙, 프롬프트 템플릿 | 300 | P0 |
| `.claude/agents/planner.md` | Planner 에이전트 정의 | 150 | P1 |
| `.claude/agents/api-designer.md` | API designer 에이전트 정의 | 150 | P1 |
| `.claude/agents/implementer.md` | Implementer 에이전트 정의 | 150 | P1 |
| `.claude/agents/tester.md` | Tester 에이전트 정의 | 150 | P1 |
| `.claude/agents/reviewer.md` | Reviewer 에이전트 정의 | 150 | P1 |
| `.claude/commands/new-feature.md` | 기능 계획 명령어 | 80 | P2 |
| `.claude/commands/new-api.md` | API 디자인 명령어 | 80 | P2 |
| `.claude/commands/run-tests.md` | 테스트 생성 명령어 | 80 | P2 |
| `.claude/commands/pre-pr.md` | PR 리뷰 명령어 | 80 | P2 |
| `.claude/skills/generate-plan/skill.md` | 계획 생성 스킬 | 200 | P3 |
| `.claude/skills/generate-api-spec/skill.md` | API 명세 생성 스킬 | 250 | P3 |
| `.claude/skills/generate-tests/skill.md` | 테스트 생성 스킬 | 300 | P3 |
| `.claude/skills/run-review/skill.md` | 코드 리뷰 스킬 | 250 | P3 |

**총 예상량**: 약 3,000줄의 설정 및 템플릿

---

## 3. 기술 스택 차이점

### 언어 & 프레임워크
| 측면 | Agentic | Unit-Simulator | 필요한 적응 |
|------|---------|----------------|-------------|
| **언어** | TypeScript | C# | 높음 - 모든 코드 템플릿 |
| **백엔드** | Express.js | ASP.NET Core | 높음 - API 패턴 다름 |
| **테스팅** | Jest | xUnit | 높음 - 테스트 생성 문법 |
| **패키지 매니저** | npm | NuGet | 중간 - 의존성 패턴 |
| **빌드 도구** | npm scripts | dotnet CLI | 중간 - 빌드 명령어 |
| **타입 시스템** | TypeScript | C# 제네릭 | 높음 - 다른 패러다임 |

### 코드 규칙
| 규칙 | Agentic (TS) | Unit-Simulator (C#) |
|------|--------------|---------------------|
| **명명** | camelCase | PascalCase (클래스), camelCase (로컬) |
| **비동기** | async/await with Promises | async/await with Task<T> |
| **Null 안전성** | `?` 선택적 타입 | Nullable 참조 타입 |
| **모듈** | ES6 import/export | C# 네임스페이스, using 문 |
| **설정** | JSON/YAML | appsettings.json, .csproj |
| **주석** | JSDoc | XML 문서 주석 |

---

## 4. 에이전트 워크플로우 비교

### Agentic 워크플로우 (Node.js/TypeScript)
```
사용자: "사용자 인증 추가"
  ↓
/new-feature → Planner → plan.md, feature.md
  ↓
/new-api → API Designer → new_api_endpoint.md (Express 라우트)
  ↓
Implementer → TypeScript 코드 (Express 컨트롤러, 미들웨어)
  ↓
/run-tests → Tester → Jest 테스트 파일
  ↓
/pre-pr → Reviewer → code-review.md, pull_ticket.md
```

### Unit-Simulator 워크플로우 (C#/.NET) - 목표
```
사용자: "타워 업그레이드 시스템 추가"
  ↓
/new-feature → Planner → plan.md, feature.md
  ↓
/new-api → API Designer → new_api_endpoint.md (WebSocket 프로토콜)
  ↓
Implementer → C# 코드 (Core 로직, Server 핸들러, DTO)
  ↓
/run-tests → Tester → xUnit 테스트 파일
  ↓
/pre-pr → Reviewer → code-review.md, pull_ticket.md
```

**주요 차이점**: REST API 대신 WebSocket 프로토콜, TypeScript 대신 C# 규칙

---

## 5. 스킬 적응 요구사항

### Skill 1: generate-plan
- **필요한 변경**: 최소
- **이유**: 계획 수립은 언어에 독립적
- **적응**: C# 프로젝트 구조, .NET 용어 참조

### Skill 2: generate-api-spec
- **필요한 변경**: 높음
- **이유**: 다른 API 패러다임 (REST → WebSocket)
- **적응**:
  - HTTP 엔드포인트 대신 WebSocket 메시지 형식
  - TypeScript 인터페이스 대신 C# DTO 정의
  - System.Text.Json 직렬화 패턴
  - 요청/응답 및 이벤트 메시지 모두 고려

### Skill 3: scaffold-endpoint
- **필요한 변경**: 매우 높음
- **이유**: 완전히 다른 코드 구조
- **적응**:
  - TypeScript 모듈 대신 C# 클래스 생성
  - Express 라우트 대신 WebSocket 핸들러 생성
  - UnitSimulator.Server 패턴 준수
  - 적절한 네임스페이스를 가진 DTO 생성
  - XML 문서 주석 포함

### Skill 4: generate-tests
- **필요한 변경**: 매우 높음
- **이유**: 다른 테스트 프레임워크 및 패턴
- **적응**:
  - Jest (describe, it) 대신 xUnit 문법 ([Fact], [Theory])
  - C# 테스트 구조 (클래스 기반)
  - expect() 대신 FluentAssertions 또는 Assert 메서드
  - 복잡한 설정을 위한 테스트 픽스처
  - 비동기 테스트 패턴 (Task<T>)

### Skill 5: run-review
- **필요한 변경**: 중간
- **이유**: 다른 코드 규칙 및 패턴
- **적응**:
  - C# 명명 규칙 확인 (PascalCase 등)
  - Task<T>를 사용한 async/await 사용 검증
  - nullable 참조 타입 주석 확인
  - XML 문서 완전성 검토
  - 의존성 추가 시 .csproj 변경사항 검토

---

## 6. 명세 문서 템플릿

### 마이그레이션할 문서 (동일한 구조)
언어에 관계없이 최소한의 변경으로 작동하는 문서:

| 문서 | 목적 | 언어 종속적? |
|------|------|-------------|
| `plan.md` | 프로젝트 계획 | ❌ 아니오 |
| `feature.md` | 기능 요구사항 | ❌ 아니오 |
| `bug.md` | 버그 추적 | ❌ 아니오 |
| `chore.md` | 유지보수 작업 | ❌ 아니오 |
| `review.md` | 기능 검증 | ❌ 아니오 |
| `pull_ticket.md` | PR 요약 | ❌ 아니오 |

### 적응이 필요한 문서

| 문서 | 필요한 변경사항 |
|------|----------------|
| `new_api_endpoint.md` | WebSocket 프로토콜 형식, C# DTO 예시, JSON 스키마 |
| `update_api_endpoint.md` | WebSocket 브레이킹 체인지, 마이그레이션 전략 |
| `test-be.md` | xUnit 예시, C# 테스트 구조 |
| `test-fe.md` | React/TypeScript (최소 - 이미 일치) |
| `code-review.md` | C#/.NET 모범 사례 체크리스트 |
| `reproduce.md` | C# 디버깅 명령어, dotnet CLI 사용 |

---

## 7. 명령어 사용 예시

### Agentic (TypeScript/Express)
```bash
# 새 기능 계획
/new-feature "JWT 인증으로 사용자 로그인 추가"

# API 디자인
/new-api
# 생성: POST /auth/login, POST /auth/register, GET /auth/profile

# 테스트 실행
/run-tests
# Jest 테스트 생성, 실행: npm test

# PR 전 리뷰
/pre-pr
# Express 컨트롤러, 미들웨어 리뷰, JWT 구현 검증
```

### Unit-Simulator (C#/.NET) - 목표
```bash
# 새 기능 계획
/new-feature "레벨 기반 스탯 스케일링을 갖는 타워 업그레이드 시스템 추가"

# API 디자인
/new-api
# 생성: WebSocket 메시지 타입, C# DTO, 직렬화

# 테스트 실행
/run-tests --project Core
# xUnit 테스트 생성, 실행: dotnet test UnitSimulator.Core.Tests

# PR 전 리뷰
/pre-pr
# C# 클래스 리뷰, 비동기 패턴, 명명 규칙 검증
```

---

## 8. 리소스 설정 차이

### Agentic mcp.json
```json
{
  "resources": {
    "specs": "./specs",
    "agents": "./.claude/agents",
    "skills": "./.claude/skills",
    "commands": "./.claude/commands",
    "apps": "./apps"
  }
}
```

### Unit-Simulator mcp.json (제안)
```json
{
  "resources": {
    "specs": "./specs",
    "agents": "./.claude/agents",
    "skills": "./.claude/skills",
    "commands": "./.claude/commands",
    "docs": "./docs",
    "core": "./UnitSimulator.Core",
    "server": "./UnitSimulator.Server",
    "models": "./ReferenceModels",
    "tests": {
      "core": "./UnitSimulator.Core.Tests",
      "models": "./ReferenceModels.Tests"
    },
    "ui": "./sim-studio",
    "data": "./data"
  }
}
```

**주요 추가사항**: 세밀한 접근을 위해 여러 C# 프로젝트를 별도로 매핑

---

## 9. 마이그레이션 복잡도 평가

### 낮은 복잡도 (직접 복사)
- ✅ 디렉토리 구조 (`.claude/agents/`, `.claude/commands/`, `.claude/skills/`)
- ✅ mcp.json 구조 (경로 적응)
- ✅ AGENTS.md 구조 (에이전트 책임 적응)
- ✅ CLAUDE.md 구조 (코드 예시 적응)
- ✅ 명세 템플릿 (plan.md, feature.md, bug.md 등)

### 중간 복잡도 (상당한 적응)
- ⚠️ 에이전트 정의 (동일한 역할, 다른 기술 세부사항)
- ⚠️ 명령어 정의 (동일한 흐름, 다른 도구)
- ⚠️ API 명세 템플릿 (REST → WebSocket)
- ⚠️ 리뷰 체크리스트 (TypeScript → C# 모범 사례)

### 높은 복잡도 (완전한 재작성)
- 🔴 generate-api-spec 스킬 (WebSocket 프로토콜 디자인)
- 🔴 scaffold-endpoint 스킬 (C# 코드 생성)
- 🔴 generate-tests 스킬 (xUnit 테스트 생성)
- 🔴 테스트 실행 로직 (npm test → dotnet test)

---

## 10. 성공 기준 비교

### Agentic 측정 결과
| 지표 | 이전 | 이후 | 개선 |
|------|------|------|------|
| 계획 문서 시간 | 수동 작성 | 자동 생성 | 80% 절감 |
| API 명세 시간 | 수동 작성 | 자동 생성 | 70% 절감 |
| 코드 스캐폴딩 | 수동 | 자동 생성 | 90% 절감 |
| 테스트 도출 | 경험 기반 | 명세 기반 | 누락 감소 |
| 문서 동기화 | 수동 | 자동 동기화 | 일관성 향상 |

### Unit-Simulator 목표 결과
| 지표 | 현재 | 목표 | 측정 방법 |
|------|------|------|-----------|
| 기능 계획 | ~2시간 | ~30분 | 시간 추적 |
| API 디자인 문서 | ~1.5시간 | ~20분 | 시간 추적 |
| 테스트 케이스 작성 | ~3시간 | ~45분 | 시간 추적 |
| PR 문서화 | ~1시간 | ~15분 | 시간 추적 |
| 코드 리뷰 준비 | ~1.5시간 | ~20분 | 시간 추적 |
| **기능당 총 시간** | ~9시간 | ~2.5시간 | **72% 감소** |

---

## 11. 리스크 비교

### Agentic (검증됨)
- ✅ Task CRUD API로 검증됨
- ✅ 전체 워크플로우 테스트됨
- ✅ 시간 절감 문서화됨
- ✅ TypeScript/Node.js 생태계

### Unit-Simulator (검증 필요)
- ⚠️ C#/.NET 생태계 (다른 패턴)
- ⚠️ WebSocket 프로토콜 (REST와 다름)
- ⚠️ 다중 프로젝트 구조 (Core/Server/Models)
- ⚠️ React UI 변경 (필요시)
- ⚠️ ReferenceModels 데이터 주도 아키텍처

**완화 방안**: 각 단계에서 검증을 포함한 단계별 접근

---

## 12. 빠른 마이그레이션 체크리스트

### Phase 1: 기초 (1주차)
- [ ] 하위 디렉토리가 있는 `specs/` 디렉토리 생성
- [ ] C# 프로젝트 매핑이 있는 `mcp.json` 작성
- [ ] .NET에 맞춘 역할로 `AGENTS.md` 작성
- [ ] C# 코드 예시가 있는 `CLAUDE.md` 작성

### Phase 2: 에이전트 (2주차)
- [ ] Planner 에이전트 정의 (`.claude/agents/planner.md`)
- [ ] API Designer 에이전트 정의 (`.claude/agents/api-designer.md`)
- [ ] Implementer 에이전트 정의 (`.claude/agents/implementer.md`)
- [ ] Tester 에이전트 정의 (`.claude/agents/tester.md`)
- [ ] Reviewer 에이전트 정의 (`.claude/agents/reviewer.md`)

### Phase 3: 명령어 (3주차)
- [ ] `/new-feature` 명령어 생성
- [ ] `/new-api` 명령어 생성
- [ ] `/run-tests` 명령어 생성
- [ ] `/pre-pr` 명령어 생성

### Phase 4: 스킬 (4-5주차)
- [ ] `generate-plan` 스킬 구현
- [ ] `generate-api-spec` 스킬 구현 (WebSocket)
- [ ] `generate-tests` 스킬 구현 (xUnit)
- [ ] `run-review` 스킬 구현 (C# 규칙)
- [ ] 선택사항: `scaffold-endpoint` 스킬 구현

### Phase 5: 테스트 (6주차)
- [ ] 테스트 기능으로 전체 워크플로우 실행
- [ ] 시간 절감 측정
- [ ] 학습 내용 문서화
- [ ] 결과 기반 개선

### Phase 6: 문서화 (7주차)
- [ ] 워크플로우 가이드 작성
- [ ] 빠른 시작 가이드 생성
- [ ] 회고 문서화
- [ ] 최종 메트릭 측정

---

## 13. 주요 요점

### 직접 전환 가능한 것
1. **에이전트 아키텍처**: 명확한 책임을 가진 5개 에이전트 구조
2. **문서 우선 접근**: 구현 전 모든 결정 문서화
3. **핸드오프 프로토콜**: 명확한 출력을 가진 순차적 에이전트 실행
4. **명령어 패턴**: CLI 명령어가 에이전트 워크플로우 트리거
5. **명세 구조**: plan.md, feature.md, test.md, review.md 템플릿

### 적응이 필요한 것
1. **코드 생성**: 모든 템플릿이 C# 문법 및 .NET 패턴 사용 필수
2. **API 디자인**: REST → WebSocket 프로토콜 디자인
3. **테스트 프레임워크**: Jest → xUnit 문법 및 구조
4. **빌드 명령어**: npm scripts → dotnet CLI 명령어
5. **규칙**: TypeScript → C# 명명, 비동기, null 안전성

### 중요한 성공 요인
1. **스킬 품질**: C# 코드 생성이 기존 패턴과 일치해야 함
2. **테스트 커버리지**: xUnit 테스트 생성이 포괄적이어야 함
3. **에이전트 프롬프트**: .NET 생태계를 깊이 이해해야 함
4. **검증**: 각 단계가 진행 전 검증되어야 함
5. **문서화**: CLAUDE.md에 C# 패턴의 명확한 예시

---

## 14. 다음 단계

1. **검토**: 전체 마이그레이션 계획 읽기 (`docs/agentic-migration-plan-ko.md`)
2. **결정**: 6단계 접근 방식 승인 또는 조정
3. **시작**: Phase 1 시작 (기초 설정)
4. **추적**: 진행 상황 추적을 위한 GitHub 프로젝트 보드 생성
5. **측정**: Phase 5 테스트 전 시간 절감을 위한 기준선 메트릭 설정

---

**문서 버전**: 1.0
**날짜**: 2026-01-06
**관련 문서**: `agentic-migration-plan-ko.md`
